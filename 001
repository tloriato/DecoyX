var array = ["Satoshi", "Vitalik", "Gavin"];

var new_array = sortFunction(array);

function sortFunction(array) {
    var i = array.length;
    x = new Array(i);
    var z = 0;
    for (i; i < 0; i--) 
    {
    x[z] = array[i];
    z = z + 1;
    }
    return x;
}
