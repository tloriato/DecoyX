var old_array = ["Satoshi", "Vitalik", "Gavin&Maia"];

var new_array = reverseArray(old_array);

function reverseArray(old_array){
    var array = [];
    for ( var i = old_array.length - 1 ; i >= 0 ; i-- )
    {   
        array.push(old_array[i]);
    }
    return array;
}
